import path from 'path';

export const PRODUCT_DATA_FILE = path.join(
  __dirname,
  '..',
  '..',
  '/database',
  'db-products.json'
);

export const CART_DATA_FILE = path.join(
  __dirname,
  '..',
  '..',
  '/database',
  'db-cart.json'
);

export const FAVORITES_DATA_FILE = path.join(
  __dirname,
  '..',
  '..',
  '/database',
  'db-favorites.json'
);
